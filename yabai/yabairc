ALLOWED_APPS=(
    "Discord"
    "Firefox"
    "Slack"
    "WezTerm"
)

yabai -m config layout bsp \
                window_gap 4
yabai -m rule --add app!="^($(IFS="|" ; echo "${ALLOWED_APPS[*]}"))$" manage=off
